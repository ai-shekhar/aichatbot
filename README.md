# aichatbot
Implements a simple chatbot for university-related queries using a Logistic Regression model and TF-IDF vectorization. The chatbot responds to user questions based on predefined intents and patterns loaded from a JSON file.
